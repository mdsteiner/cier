# the guidance message renders the locked text

    Code
      invisible(cier_psychsyn_critval(bfi, critical_r = c(0.6, 0.5)))
    Message
      No synonyms clear `critical_r` = 0.6 (strongest inter-item r = 0.59).
      i Lower `critical_r` to surface pairs and score more respondents.

