% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-gnormed.R
\name{cier_gnormed}
\alias{cier_gnormed}
\title{Normed polytomous Guttman-error (Gnormed) C/IER index}
\usage{
cier_gnormed(responses, items, fpr = NULL, cutoff = NULL, seed = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{items}{Item-metadata data.frame, one row per item aligned to the columns of
\code{responses}. Needs an integer \code{max} column (largest response option) on every
item, all sharing the span \code{max - min} (\code{Ncat = max - min + 1 >= 2}). Optional
logical \code{reverse_keyed} (default none) and integer \code{min} (scale base, default
\code{1}). When \code{responses} has column names, an optional \code{item} column (or matching
row names) is cross-checked, so a reordered frame is a typed error.}

\item{fpr}{Optional nominal level for the Monte-Carlo null cutoff, a number in
\verb{(0, 1)}; \code{NULL} (default) uses \code{0.05}. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional literal cutoff on the score, a number in \verb{[0, 1]};
respondents at or above it are flagged.}

\item{seed}{Optional integer making the Monte-Carlo null reproducible; applied
locally and restores the session's random state. \code{NULL} (default) draws from the
ambient random stream, so cutoff and flags can vary. Ignored when \code{cutoff} is
supplied.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's normed polytomous Guttman-error score (Emons, 2008;
Molenaar, 1991). Ordering the item-steps by descending popularity, a Guttman error
endorses a less popular step while missing a more popular one; the count is normed
by the maximum possible given the respondent's total step score. Aberrant patterns
score high -- high values flag carelessness. Nonparametric (no item response model
is fitted).
}
\details{
\strong{Computation.} Item-step popularity positions normed by an n-independent max-plus
knapsack maximum. Reverse-keyed items (\code{items$reverse_keyed}) are reverse-scored
with \code{(min + max) - x} (\code{min} defaults to \code{1}), then recoded to the \verb{0..(Ncat - 1)}
contract; supply raw responses and declare reverse items through \code{items}. Scores a
single category count (\code{Ncat = max - min + 1}), so every item must share the same
\code{max - min} span (the base may differ: a \verb{1..5} and a \verb{0..4} item score together).
Mixed-format data raises a typed error with a dedicated subclass, so \code{\link[=cier_screen]{cier_screen()}}
skips Gnormed rather than aborting the battery -- score homogeneous subsets
separately.

\strong{Cutoff -- a Monte-Carlo null.} The default references a sum-score-conditional
nonparametric resample (resample observed sum scores, then draw each item from its
category frequencies among respondents at that score), scored by the same closed
form and summarised by the upper-tail bootstrap quantile at level \code{fpr}. Being a
real null, the flag rate is not pinned at \code{fpr}. With \code{seed = NULL} the cutoff and
flags can vary run to run; pass an integer \code{seed} to fix them (applied locally).
Override with one of \code{fpr} or \code{cutoff}.

\strong{The sample must span the declared scale.} Item-step popularities are undefined
when a scale end never occurs, so the complete-case block must attain both the
declared \code{min} and \code{max}. Otherwise a typed error is raised (same subclass as the
mixed-format case, so \code{\link[=cier_screen]{cier_screen()}} skips rather than aborts).

\strong{Abstention.} Computed on complete cases, so any missing cell excludes a
respondent (\code{value}, \code{flagged} are \code{NA}). With fewer than two complete respondents
or fewer than three items, every \code{value} is \code{NA}, a warning is emitted, and no one
is flagged.
}
\section{What this catches}{

Aberrant, inconsistent patterns -- random and some patterned responding -- with
many Guttman reversals. Blind spot: straightlining scores a low, non-aberrant value
rather than abstaining, so it evades this index -- pair with \code{\link[=cier_longstring]{cier_longstring()}} and
\code{\link[=cier_irv]{cier_irv()}}.
}

\examples{
# Columns 1:44 are the BFI items (5-point, 1..5); a trailing "_R" marks reverse.
nm <- names(bfi_careless)[1:44]
items <- data.frame(reverse_keyed = grepl("_R$", nm), max = 5L)
out <- cier_gnormed(bfi_careless[, 1:44], items, seed = 1)
out
head(as.data.frame(out))
}
\references{
Niessen, A. S. M., Meijer, R. R., & Tendeiro, J. N. (2016). Detecting careless
respondents in web-based questionnaires: Which method to use? \emph{Journal of
Research in Personality}, 63, 1–11. \doi{10.1016/j.jrp.2016.04.010}

Emons, W. H. M. (2008). Nonparametric person-fit analysis of polytomous item
scores. \emph{Applied Psychological Measurement}, 32(3), 224–247.

Molenaar, I. W. (1991). A weighted Loevinger H-coefficient extending Mokken
scaling to multicategory items. \emph{Kwantitatieve Methoden}, 12(37), 97–117.
}
\seealso{
\code{\link[=cier_longstring]{cier_longstring()}}, \code{\link[=cier_irv]{cier_irv()}}

Other person-fit indices:
\code{\link[=cier_ht]{cier_ht()}}
}
\concept{person-fit indices}
