% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-gnormed.R
\name{cier_gnormed}
\alias{cier_gnormed}
\title{Normed polytomous Guttman-error (Gnormed) C/IER index}
\usage{
cier_gnormed(responses, items, fpr = NULL, cutoff = NULL, seed = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{items}{A data.frame of item metadata, one row per item, aligned to the
columns of \code{responses}. Must carry an integer \code{categories} column (the number
of response options) that is the \strong{same} value (\verb{>= 2}) on every item. An
optional logical \code{reverse_keyed} column marks reverse-keyed items (default:
none). An optional integer \code{min} column gives the smallest response option
(the scale base; default \code{1}) -- declare it for a 0-based or bipolar scale.}

\item{fpr}{Optional nominal level for the Monte-Carlo null cutoff (PerFit's
\code{Blvl}). \code{NULL} (default) uses the registry default \code{0.05}. A finite number
in the open interval \verb{(0, 1)}. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the score, supplied instead of
\code{fpr}. A single finite number in \verb{[0, 1]}; respondents whose Gnormed value is
at or above it are flagged (the upper-tail direction).}

\item{seed}{Optional integer making the Monte-Carlo null cutoff reproducible.
\code{NULL} (default) draws from the ambient random stream (the cutoff then varies
per call); a non-\code{NULL} seed is applied locally and restores your session's
random state on exit. Ignored when a literal \code{cutoff} is supplied.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{normed polytomous Guttman-error} score (Emons,
2008; Molenaar, 1991) via \code{\link[PerFit:Gnormed.poly]{PerFit::Gnormed.poly()}}. Ordering the item-steps by
descending popularity, a Guttman error is an endorsement of a less popular step
while a more popular one is missed; the count is normed by the maximum possible
given the respondent's total step score. A careful respondent produces few
such reversals, so a careless (aberrant) response pattern scores \strong{high} --
high values flag carelessness. The statistic is \strong{nonparametric} (no item
response model is fitted).
}
\details{
\strong{PerFit is required.} Gnormed is scored by \code{\link[PerFit:Gnormed.poly]{PerFit::Gnormed.poly()}}, so the
\code{PerFit} package must be installed; the index aborts with an informative error
otherwise. Reverse-keyed items (\code{items$reverse_keyed}) are reverse-scored first
with the self-inverse reflection \code{(min + max) - x} (where
\code{max = min + categories - 1}, and \code{min} defaults to \code{1}, i.e. the classic
\code{(categories + 1) - x}); the responses are then recoded to PerFit's documented
\verb{0..(Ncat - 1)} contract. Supply the raw responses and declare reverse items
through \code{items}. PerFit requires a single number of response categories, so
\code{items$categories} must be the same integer (\verb{>= 2}) on every item.

\strong{Cutoff -- the PerFit Monte-Carlo null.} Unlike the sample-percentile
indices, Gnormed's default cutoff is referenced to a \strong{simulated null}: PerFit
resamples model-conforming response vectors and takes the nominal-rate
(\code{Blvl = fpr}) quantile of the statistic (\code{\link[PerFit:cutoff]{PerFit::cutoff()}}). Respondents
beyond the upper tail are flagged. Because this is a real null rather than a
ranking convention, the flag rate is \strong{not} pinned at \code{fpr}: it is whatever
share of respondents exceed the simulated quantile, and can be informatively
higher under contamination. The simulation is randomised; pass an integer
\code{seed} for a reproducible cutoff (it is applied locally and does not disturb
your session's random stream). Override the cutoff with \strong{one} of two mutually
exclusive arguments: \code{fpr} (the nominal level) or \code{cutoff} (a literal threshold
on the score).

\strong{Abstention.} Because \code{\link[PerFit:Gnormed.poly]{PerFit::Gnormed.poly()}} needs complete data, a
respondent with \strong{any} missing cell is excluded: both \code{value} and \code{flagged}
are \code{NA} and the row is dropped from the flag count and rate. If fewer than two
respondents are complete, or fewer than three items are present (PerFit's
recursive denominator needs \verb{>= 3}), every \code{value} is \code{NA}, a warning is
emitted, and no one is flagged.
}
\section{What this catches}{

Aberrant, inconsistent response patterns -- random and some patterned
responding -- that produce many Guttman reversals against the sample's
item-step popularities. It has a \strong{documented blind spot}: a straightliner is
scored (it receives a \emph{low}, non-aberrant value) rather than abstaining, so
straightlining evades this index -- pair it with \code{\link[=cier_longstring]{cier_longstring()}} and
\code{\link[=cier_irv]{cier_irv()}}.
}

\examples{
\dontshow{if (requireNamespace("PerFit", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# The 44 BFI items are the first 44 columns of the bundled example data; they
# are 5-point items. Build item metadata from the column names: a trailing "_R"
# marks a reverse-keyed item.
nm <- names(bfi_careless)[1:44]
items <- data.frame(reverse_keyed = grepl("_R$", nm), categories = 5L)
out <- cier_gnormed(bfi_careless[, 1:44], items, seed = 1)
out
head(as.data.frame(out))
\dontshow{\}) # examplesIf}
}
\references{
Niessen, A. S. M., Meijer, R. R., & Tendeiro, J. N. (2016). Detecting careless
respondents in web-based questionnaires: Which method to use? \emph{Journal of
Research in Personality}, 63, 1–11. \doi{10.1016/j.jrp.2016.04.010}

Emons, W. H. M. (2008). Nonparametric person-fit analysis of polytomous item
scores. \emph{Applied Psychological Measurement}, 32(3), 224–247.

Molenaar, I. W. (1991). A weighted Loevinger H-coefficient extending Mokken
scaling to multicategory items. \emph{Kwantitatieve Methoden}, 12(37), 97–117.
}
\seealso{
\code{\link[PerFit:Gnormed.poly]{PerFit::Gnormed.poly()}}, \code{\link[=cier_longstring]{cier_longstring()}}, \code{\link[=cier_irv]{cier_irv()}}

Other person-fit indices:
\code{\link[=cier_ht]{cier_ht()}}
}
\concept{person-fit indices}
