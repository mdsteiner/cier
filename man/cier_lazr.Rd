% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-lazr.R
\name{cier_lazr}
\alias{cier_lazr}
\title{Laz.R first-order Markov predictability C/IER index}
\usage{
cier_lazr(responses, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of integer-coded responses, one row per respondent and one
column per item, in administration order. \code{NA} marks a missing response.}

\item{fpr}{Optional target false-positive tail mass for the percentile cutoff.
\code{NULL} (default) uses the registry default \code{0.05}. A finite number in the
open interval \verb{(0, 1)}; the cutoff is that upper-tail quantile of the
observed scores. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the Laz.R score, supplied instead
of \code{fpr}. A single finite number in \verb{[0, 1]}; respondents whose score is at
or above it are flagged.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{Laz.R} score (Biemann, Koch-Bayram,
Meier-Barthold & Aguinis, 2025): the average probability with which their
previous answer predicts the next. Over each respondent's response sequence
the function builds the transition-count matrix \eqn{T} and its row-normalised
probability matrix \eqn{P}, and returns
\deqn{\mathrm{Laz.R} = \frac{\sum_{i,j} P_{ij}\,T_{ij}}{n_{\mathrm{trans}}}}
the count-weighted average transition probability, bounded by roughly
\eqn{1/s} and \eqn{1} for an \eqn{s}-anchor scale. A respondent who repeats a
predictable pattern (straightlining, midpoint or extreme locking,
diagonal-lining \code{1-2-3-4-5-1-2-...}, or a seesaw \code{1-5-1-5-...}) has a high
Laz.R, so \strong{high} values flag carelessness. The score is computed on the
\strong{raw} response row in administration order -- there is no reverse-keying and
no scale blocking.
}
\details{
\strong{Cutoff.} The default flags the most predictable respondents: the cutoff is
the empirical upper \code{fpr} quantile of the observed scores (the 95th percentile
by default). This is a deliberate divergence from Biemann et al.'s
sample-specific Kneedle elbow -- it follows cier's single-\code{fpr}-knob ranking
convention, and the Laz.R score's documented dependence on sequence length
makes an absolute cutoff indefensible. Adjust the target tail with \code{fpr}, or
pass an absolute \code{cutoff} in \verb{[0, 1]} to flag on a literal Laz.R magnitude;
\code{fpr} and \code{cutoff} are mutually exclusive.

\strong{Integer anchors.} Laz.R scores a first-order Markov chain over discrete
response anchors, so responses must be integer-coded; non-integer (averaged or
POMP-rescaled) input is a typed error rather than being silently dropped. The
value is invariant to the anchor count and to the scale base: a 0-based or
bipolar coding, and any unused higher anchor, score identically.

\strong{Missing data.} A transition is counted only when both of its endpoints are
present; a missing response drops the two transitions it touches, and the
denominator is the count of valid transitions (not the item count minus one).
A respondent with fewer than \strong{two} valid transitions abstains -- both
\code{value} and \code{flagged} are \code{NA} and the row is excluded from the flag count and
rate -- because a single transition gives the uninformative \code{1} regardless of
the pattern. This drops missing transitions rather than treating a gap as a
predictable state, so a careful but incomplete respondent is not flagged.
Numeric missing codes (\code{99}, \code{-9}, and the like) must be recoded to \code{NA}
first; otherwise they are scored as genuine response anchors and add spurious
transitions.

\strong{Assumptions.} The index reads the response columns as an ordered sequence
on a common response scale, so it is meaningful only when the columns are in
administration order and share the same response options. Biemann et al.
recommend at least 20 items and a uniform anchor count across items; on a
mixed-format survey, score each same-format block in a separate call.
}
\section{What this catches}{

Predictable / repetitive responding -- straightlining, midpoint and extreme
locking, diagonal-lining, and seesaw / alternating patterns -- whose next
answer is well predicted by the previous. It is \strong{not} useful against uniform
random responding, whose Laz.R is low by design; pair it with \code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}}, or \code{\link[=cier_psychsyn]{cier_psychsyn()}}, which catch random responding by
other means.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
out <- cier_lazr(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Biemann, T., Koch-Bayram, I. F., Meier-Barthold, M., & Aguinis, H. (2025).
Using Markov chains to detect careless responding in survey research.
\emph{Organizational Research Methods}, 28(4), 543-568.
\doi{10.1177/10944281251334778}
}
\seealso{
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}}, \code{\link[=cier_longstring]{cier_longstring()}}

Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
