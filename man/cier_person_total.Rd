% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-person-total.R
\name{cier_person_total}
\alias{cier_person_total}
\title{Person-total correlation (r_pbis) C/IER index}
\usage{
cier_person_total(responses, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{fpr}{Optional lower-tail mass for the percentile cutoff, in \verb{(0, 1)}; \code{NULL}
(default) uses \code{0.05}. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional literal cutoff on the correlation, in \verb{[-1, 1]}; respondents
at or below it are flagged. Mutually exclusive with \code{fpr}.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's person-total correlation -- the Pearson correlation
between their item responses and the per-item means across the whole sample (the
"group-determined item difficulties" of Donlon & Fischer, 1968). A careless
pattern decouples from the group profile, driving the correlation toward zero, so
low values flag carelessness. Computed over the raw response row, so unlike the
split-half consistency indices it also works on a single scale.
}
\details{
Item-total form (per-item mean taken over all respondents, including the scored
one): numerically near-identical to the item-rest leave-one-out variant on real
data and far cheaper to vectorise. Keying-insensitive because the per-item means
are empirical, so it takes no item metadata.

The default flags the lowest-correlation respondents at the \code{fpr} quantile (a
ranking convention that flags at least \code{fpr} of respondents, not a calibrated
false-positive rate). Adjust with \code{fpr}, or pass an absolute \code{cutoff} in \verb{[-1, 1]}.

A respondent answering fewer than three items, or whose answered responses have
zero variance (a straightliner), has no defined correlation (\code{value} and \code{flagged}
are \code{NA}). When all item means are equal (e.g. a perfectly balanced design) every
respondent abstains with the standard insufficient-scores warning.
}
\section{What this catches}{

Random and partial / changepoint responding and idiosyncratic patterns (diagonal,
alternating) whose profile diverges from the sample's item-mean profile. It misses
pure straightlining and midpoint locking (a constant row abstains); pair it with
\code{\link[=cier_longstring]{cier_longstring()}} and \code{\link[=cier_irv]{cier_irv()}}.
}

\examples{
# The 44 BFI items are the first 44 columns of the example data.
out <- cier_person_total(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Donlon, T. F., & Fischer, F. E. (1968). An index of an individual's agreement
with group-determined item difficulties. \emph{Educational and Psychological
Measurement}, 28(1), 105–113. \doi{10.1177/001316446802800110}

Curran, P. G. (2016). Methods for the detection of carelessly invalid
responses in survey data. \emph{Journal of Experimental Social Psychology}, 66,
4–19. \doi{10.1016/j.jesp.2015.07.006}
}
\seealso{
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}}, \code{\link[=cier_irv]{cier_irv()}}

Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
