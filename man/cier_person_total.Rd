% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-person-total.R
\name{cier_person_total}
\alias{cier_person_total}
\title{Person-total correlation (r_pbis) C/IER index}
\usage{
cier_person_total(responses, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{fpr}{Optional target false-positive tail mass for the percentile
cutoff. \code{NULL} (default) uses the registry default \code{0.05}. A finite number
in the open interval \verb{(0, 1)}; the cutoff is that lower-tail quantile of the
observed scores. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the correlation, supplied instead
of \code{fpr}. A single finite number in \verb{[-1, 1]}; respondents whose
person-total correlation is at or below it are flagged (the lower-tail
direction). Use it to apply an absolute threshold rather than a sample
percentile.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{person-total correlation} -- the Pearson
correlation between their item responses and the per-item means computed
across the whole sample (the "group-determined item difficulties" of Donlon &
Fischer, 1968). A careful respondent endorses items roughly in line with the
group, so their pattern tracks the item-mean profile and the correlation is
high; a careless respondent's pattern decouples from the group, driving the
correlation toward zero. \strong{Low} values therefore flag carelessness. The score
is computed over the raw response row (no scale blocking), so -- unlike the
split-half consistency indices -- it also works on a single scale.
}
\details{
\strong{Item-total, raw, keying-insensitive.} This is the item-\strong{total} form (the
per-item mean is taken over \emph{all} respondents, including the respondent being
scored, as in \code{PerFit::r.pbis}); it is numerically near-identical to the
item-rest (leave-one-out) variant on real data and vectorises far more
cheaply. Because the per-item means are empirical, the index is
\strong{keying-insensitive}: it is computed on the raw responses with no
reverse-scoring, so it takes no item metadata and a reverse-keyed item does
not need to be declared.

\strong{Cutoff.} The default flags the lowest-correlation respondents: the cutoff is
the empirical \code{fpr} quantile of the observed scores (the 5th percentile by
default) and respondents at or below it are flagged. This is a \strong{ranking}
convention -- a sample percentile flags \code{fpr} of respondents by construction --
not a calibrated false-positive rate. Adjust the target tail with \code{fpr}, or
pass an absolute \code{cutoff} in \verb{[-1, 1]} to flag on a literal correlation
threshold (e.g. one carried over from a calibration sample). \code{fpr} and \code{cutoff}
are mutually exclusive.

\strong{Abstention.} A respondent who answered fewer than three items, or whose
answered responses have zero variance (a straightliner), has no defined
correlation: both \code{value} and \code{flagged} are \code{NA} and the row is excluded from
the flag count and rate. An item answered by nobody drops out rather than
poisoning the computation.
}
\section{What this catches}{

Random and partial / changepoint responding and idiosyncratic patterns
(diagonal, alternating) whose profile diverges from the sample's item-mean
profile. It \strong{misses} pure straightlining and midpoint locking (a constant
row has zero variance and abstains); pair it with \code{\link[=cier_longstring]{cier_longstring()}} and
\code{\link[=cier_irv]{cier_irv()}}.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
out <- cier_person_total(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Donlon, T. F., & Fischer, F. E. (1968). An index of an individual's agreement
with group-determined item difficulties. \emph{Educational and Psychological
Measurement}, 28(1), 105–113. \doi{10.1177/001316446802800110}

Curran, P. G. (2016). Methods for the detection of carelessly invalid
responses in survey data. \emph{Journal of Experimental Social Psychology}, 66,
4–19. \doi{10.1016/j.jesp.2015.07.006}
}
\seealso{
\code{\link[PerFit:r.pbis]{PerFit::r.pbis()}}, \code{\link[=cier_mahalanobis]{cier_mahalanobis()}}, \code{\link[=cier_irv]{cier_irv()}}

Other indirect indices:
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
