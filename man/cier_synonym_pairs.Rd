% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-pairing-diagnostics.R
\name{cier_synonym_pairs}
\alias{cier_synonym_pairs}
\title{List the synonym (or antonym) item pairs and their inter-item correlations}
\usage{
cier_synonym_pairs(responses, critical_r = NULL, antonym = FALSE)
}
\arguments{
\item{responses}{Numeric matrix (or data.frame / tibble coerced internally),
one row per respondent, one column per item.}

\item{critical_r}{\code{NULL} (default) to list every pair, or a single number in
\verb{(0, 1)} to keep only pairs whose inter-item correlation clears that magnitude.}

\item{antonym}{Single logical. \code{FALSE} (default) lists positive synonym pairs
descending by correlation; \code{TRUE} lists negative antonym pairs ascending (most
negative first).}
}
\value{
A data frame, one row per pair: \code{item_i}, \code{item_j} (integer column
indices, \code{item_i > item_j}), \code{name_i}, \code{name_j} (item names, or \code{NA} when
\code{responses} has no column names), and \code{r} (signed inter-item correlation).
}
\description{
Surfaces the whole-sample inter-item correlations that \code{\link[=cier_psychsyn]{cier_psychsyn()}} (and the
antonyms index) use to discover item pairs, so you can see which pairs qualify at a
candidate \code{critical_r} -- or the full distribution -- and choose a threshold. On broad
inventories the strongest correlations can fall below the default \code{0.60}, leaving no
synonym pairs; this makes that visible.
}
\details{
Pairs are the lower triangle of the whole-sample correlation matrix, each appearing once
with the larger item index first (\code{item_i > item_j}). With \code{critical_r} set, the result
is exactly the pairs \code{\link[=cier_psychsyn]{cier_psychsyn()}} discovers from the whole sample at that threshold;
\code{critical_r = NULL} lists every non-missing pair. No \code{reference} argument, so to inspect a
\code{cier_psychsyn(reference = ...)} run pass that subset/sample as \code{responses}. A constant
(zero-variance) item is omitted; a threshold no pair clears returns a zero-row frame.
}
\examples{
# BFI correlations fall below 0.60, so list pairs qualifying at a lower threshold:
head(cier_synonym_pairs(bfi_careless[, 1:44], critical_r = 0.5))
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437–455. \doi{10.1037/a0028085}
}
\seealso{
\code{\link[=cier_psychsyn_critval]{cier_psychsyn_critval()}} for the coverage sweep, \code{\link[=cier_psychsyn]{cier_psychsyn()}} for the
index.

Other pairing diagnostics:
\code{\link[=cier_psychsyn_critval]{cier_psychsyn_critval()}}
}
\concept{pairing diagnostics}
