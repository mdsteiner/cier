% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-pairing-diagnostics.R
\name{cier_synonym_pairs}
\alias{cier_synonym_pairs}
\title{List the synonym (or antonym) item pairs and their inter-item correlations}
\usage{
cier_synonym_pairs(responses, critical_r = NULL, antonym = FALSE)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.}

\item{critical_r}{Either \code{NULL} (default) to list every pair, or a single
number in the open interval \verb{(0, 1)} to keep only the pairs whose inter-item
correlation clears that magnitude.}

\item{antonym}{Single logical. \code{FALSE} (default) lists positively correlated
synonym pairs, sorted by descending correlation; \code{TRUE} lists negatively
correlated antonym pairs, sorted by ascending (most negative) correlation.}
}
\value{
A data frame with one row per pair and columns \code{item_i}, \code{item_j}
(integer column indices, \code{item_i > item_j}), \code{name_i}, \code{name_j} (the item
names, or \code{NA} when \code{responses} has no column names), and \code{r} (the signed
inter-item correlation).
}
\description{
Surfaces the whole-sample inter-item correlations that \code{\link[=cier_psychsyn]{cier_psychsyn()}} (and
the antonyms index) use to discover item pairs, so you can see which pairs
qualify at a candidate \code{critical_r} -- or the full distribution -- and choose a
threshold. On broad inventories the strongest inter-item correlations can fall
below the default \code{0.60}, leaving no synonym pairs; this diagnostic makes that
visible.
}
\details{
The pairs are the lower triangle of the whole-sample correlation matrix
(\code{stats::cor(responses, use = "pairwise.complete.obs")}), so each pair appears
once with the larger item index first (\code{item_i > item_j}). With \code{critical_r}
set, the returned set is exactly the pairs \code{\link[=cier_psychsyn]{cier_psychsyn()}} scores at that
threshold: synonym pairs whose inter-item correlation exceeds \code{critical_r}, or
antonym pairs (\code{antonym = TRUE}) whose correlation is below \code{-critical_r}. With
\code{critical_r = NULL} every non-missing pair is listed.
A pair that involves a constant (zero-variance) item has an undefined
correlation and is omitted. A threshold that no pair clears returns a zero-row
frame (not an error).
}
\examples{
# The BFI's strongest inter-item correlations fall below 0.60, so list the
# pairs that qualify at a lower threshold:
head(cier_synonym_pairs(bfi_careless[, 1:44], critical_r = 0.5))
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437–455. \doi{10.1037/a0028085}
}
\seealso{
\code{\link[=cier_psychsyn_critval]{cier_psychsyn_critval()}} for the coverage sweep, \code{\link[=cier_psychsyn]{cier_psychsyn()}}
for the index, and \code{\link[careless:psychsyn_critval]{careless::psychsyn_critval()}}.

Other pairing diagnostics:
\code{\link[=cier_psychsyn_critval]{cier_psychsyn_critval()}}
}
\concept{pairing diagnostics}
