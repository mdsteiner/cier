% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-pairing-diagnostics.R
\name{cier_psychsyn_critval}
\alias{cier_psychsyn_critval}
\title{Sweep \code{critical_r} to weigh synonym pairs against respondent coverage}
\usage{
cier_psychsyn_critval(
  responses,
  critical_r = c(0.7, 0.65, 0.6, 0.55, 0.5, 0.45, 0.4, 0.35, 0.3),
  antonym = FALSE
)
}
\arguments{
\item{responses}{Numeric matrix (or data.frame / tibble coerced internally),
one row per respondent, one column per item.}

\item{critical_r}{Numeric vector of candidate thresholds, each in \verb{(0, 1)}.
Defaults to a grid from \code{0.70} down to \code{0.30}.}

\item{antonym}{Single logical. \code{FALSE} (default) sweeps the synonym (positive)
tail; \code{TRUE} sweeps the antonym (negative) tail.}
}
\value{
A data frame, one row per \code{critical_r}: \code{critical_r}, \code{n_pairs},
\code{n_scored}, \code{n_abstain}, \code{abstention_rate} (share of respondents with no usable
score, rounded to 3 dp), and \code{strongest_r} (strongest in-tail inter-item
correlation, rounded to 3 dp; constant across rows, signed -- most positive for
synonyms, most negative for antonyms).
}
\description{
Evaluates a grid of candidate \code{critical_r} values, reporting for each how many synonym
(or antonym) item pairs qualify and how many respondents become scorable by
\code{\link[=cier_psychsyn]{cier_psychsyn()}}. Lowering \code{critical_r} admits more pairs, usually scoring more
respondents at the cost of weaker pairs -- this table makes that trade legible.
}
\details{
\code{critical_r} is a property of the inventory, not a calibrated cutoff: it sets which item
pairs count as near-synonyms. The sweep reuses the production pairing and scoring on the
whole sample, so coverage is exactly what the index produces under its default
\code{reference = NULL}. No \code{reference} argument; to weigh coverage for a
\code{cier_psychsyn(reference = ...)} run, pass that subset/sample as \code{responses}.
\code{strongest_r} is the strongest in-tail inter-item correlation, constant across the grid
(most positive for synonyms, most negative for antonyms), so it gives the lowest
threshold that can find any pair. When even the strictest grid value finds no pairs, a
typed message (\code{cier_message_no_pairs}) points you to lower \code{critical_r}.
}
\examples{
# On the BFI the default 0.60 finds no pairs; the sweep shows where coverage appears:
cier_psychsyn_critval(bfi_careless[, 1:44])
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437–455. \doi{10.1037/a0028085}
}
\seealso{
\code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}} to list the pairs themselves, \code{\link[=cier_psychsyn]{cier_psychsyn()}} for
the index.

Other pairing diagnostics:
\code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}}
}
\concept{pairing diagnostics}
