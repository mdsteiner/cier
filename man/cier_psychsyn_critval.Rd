% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-pairing-diagnostics.R
\name{cier_psychsyn_critval}
\alias{cier_psychsyn_critval}
\title{Sweep \code{critical_r} to weigh synonym pairs against respondent coverage}
\usage{
cier_psychsyn_critval(
  responses,
  critical_r = c(0.7, 0.65, 0.6, 0.55, 0.5, 0.45, 0.4, 0.35, 0.3),
  antonym = FALSE
)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.}

\item{critical_r}{A numeric vector of candidate thresholds, each in the open
interval \verb{(0, 1)}. Defaults to a grid from \code{0.70} down to \code{0.30}.}

\item{antonym}{Single logical. \code{FALSE} (default) sweeps the synonym (positive)
tail; \code{TRUE} sweeps the antonym (negative) tail.}
}
\value{
A data frame with one row per \code{critical_r} value and columns
\code{critical_r}, \code{n_pairs}, \code{n_scored}, \code{n_abstain}, \code{abstention_rate} (the
share of respondents with no usable score, rounded to 3 dp), and
\code{strongest_r} (the strongest in-tail inter-item correlation, rounded to 3 dp;
constant across rows).
}
\description{
Evaluates a grid of candidate \code{critical_r} values and reports, for each, how
many synonym (or antonym) item pairs qualify and how many respondents become
scorable by \code{\link[=cier_psychsyn]{cier_psychsyn()}}. Lowering \code{critical_r} admits more pairs, which
usually scores more respondents (more reliable person estimates) at the cost of
weaker pairs -- this table makes that trade legible in one call, and directly
answers "how many more datapoints if I lower the cutoff?".
}
\details{
\code{critical_r} is a property of the \strong{inventory}, not a calibrated cutoff: it
sets which item pairs count as near-synonyms. The sweep reuses the production
pairing and scoring (\code{find_item_pairs()} for \code{n_pairs}, \code{\link[=cier_psychsyn]{cier_psychsyn()}}'s
scorer for \code{n_scored}), so the coverage shown is exactly what the index would
produce. \code{strongest_r} -- the strongest in-tail inter-item correlation -- is
constant across the grid and tells you the lowest threshold that can find any
pair. When even the strictest grid value finds no pairs, a typed message
(\code{cier_message_no_pairs}) points you to lower \code{critical_r}.
}
\examples{
# On the BFI the default 0.60 finds no pairs; the sweep shows where coverage
# appears as the threshold drops:
cier_psychsyn_critval(bfi_careless[, 1:44])
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437–455. \doi{10.1037/a0028085}
}
\seealso{
\code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}} to list the pairs themselves, \code{\link[=cier_psychsyn]{cier_psychsyn()}}
for the index, and \code{\link[careless:psychsyn_critval]{careless::psychsyn_critval()}}.

Other pairing diagnostics:
\code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}}
}
\concept{pairing diagnostics}
