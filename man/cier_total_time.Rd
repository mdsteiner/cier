% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-total-time.R
\name{cier_total_time}
\alias{cier_total_time}
\title{Total survey completion-time C/IER index}
\usage{
cier_total_time(seconds, fpr = NULL, cutoff = NULL, frac_median = NULL)
}
\arguments{
\item{seconds}{A numeric vector of total completion times in seconds, one per
respondent. Must be strictly positive; \code{NA} marks a missing duration (which
abstains).}

\item{fpr}{Optional target false-positive tail mass for the percentile cutoff.
\code{NULL} (default) uses the registry default \code{0.05}. A finite number in the
open interval \verb{(0, 1)}; the cutoff is that lower-tail quantile of the
observed times. Mutually exclusive with \code{cutoff} and \code{frac_median}.}

\item{cutoff}{Optional \strong{literal} cutoff in seconds, supplied instead of a
rate. A single finite number \verb{>= 0}; respondents whose time is at or below it
are flagged. Mutually exclusive with \code{fpr} and \code{frac_median}.}

\item{frac_median}{Optional \strong{median-relative} cutoff: a single finite number
in \verb{(0, 1]}, resolving to \code{frac_median * median} of the finite times.
Respondents at or below it are flagged. Mutually exclusive with \code{fpr} and
\code{cutoff}.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Takes each respondent's \strong{total completion time} in seconds -- one duration
per respondent, as survey platforms export it -- and flags the fastest as
likely speeders (Ward & Meade, 2023; Huang et al., 2012). The value is the
completion time itself, so \strong{low} totals flag carelessness
(\code{direction = "lower"}); there is no response matrix and no transformation of
the input.
}
\details{
\strong{Input.} \code{seconds} is a bare numeric vector, one total per respondent. A
two-dimensional input (a matrix or data frame) is rejected -- sum your
per-cell response times to one total per respondent first. Each time must be
strictly positive; a zero or negative duration is a typed error, and a missing
duration (\code{NA}) abstains.

\strong{Cutoff.} The default flags the fastest respondents: the cutoff is the
empirical lower \code{fpr} quantile of the observed times (the 5th percentile by
default), and respondents at or below it are flagged. This is a \strong{ranking}
convention -- a sample percentile flags \code{fpr} of respondents by construction --
not a calibrated false-positive rate. Override it with \strong{one} of three
mutually exclusive arguments:
\itemize{
\item \code{fpr}, a different target tail mass for the percentile;
\item \code{frac_median}, a \strong{median-relative} rule flagging respondents faster than
that fraction of the sample median (the published instrument-free speeding
rules: Leiner's 2019 Relative Speed Index flags those slower than half the
median, i.e. \code{frac_median = 0.5}; Greszki et al. 2015 use 0.5 / 0.4 / 0.3).
Because it is anchored to the median it is robust to up to half the sample
responding carelessly, unlike the empirical percentile;
\item \code{cutoff}, a literal threshold in seconds.
Respondents whose time is at or below the resolved cutoff are flagged.
}

\strong{Abstention.} A respondent with a missing time (\code{NA}) abstains: both \code{value}
and \code{flagged} are \code{NA} and the row is excluded from the flag count and rate.
If no respondent has a finite time the cutoff cannot be resolved: it is \code{NA},
nobody is flagged, and a typed warning is raised.

\strong{Assumptions.} The index reads \code{seconds} as whole-survey durations on a
common clock; it cannot see \emph{within}-survey speeding (a respondent who answers
the first half attentively and races the second half keeps a plausible total).
Biemann et al. (2025) report that 46-80\% of whole-survey straightliners pass a
2 s/item screen, so completion time must be paired with a response-content
index. Pair it with \code{\link[=cier_irv]{cier_irv()}} or \code{\link[=cier_mahalanobis]{cier_mahalanobis()}}, and with a
page-level timing index when page times are available.
}
\section{What this catches}{

Respondents who rushed the whole survey -- the canonical speeder profile, the
strongest single signal on fully-careless protocols (Ward & Meade, 2023).
}

\section{What this misses}{

Within-survey speeding bursts, and careless respondents who took their time.
Completion time is a \emph{floor} on effort, not a content check.
}

\examples{
# One total completion time in seconds per respondent.
secs <- c(420, 380, 95, 510, 47, 460, 405, 88, 390, 430)
cier_total_time(secs)

# Flag respondents faster than half the sample median (Leiner 2019 RSI):
cier_total_time(secs, frac_median = 0.5)
}
\references{
Ward, M. K., & Meade, A. W. (2023). Dealing with careless responding in survey
data. \emph{Annual Review of Psychology}, 74, 577-596.
\doi{10.1146/annurev-psych-040422-045007}

Huang, J. L., Curran, P. G., Keeney, J., Poposki, E. M., & DeShon, R. P.
(2012). Detecting and deterring insufficient effort responding to surveys.
\emph{Journal of Business and Psychology}, 27(1), 99-114.

Leiner, D. J. (2019). Too fast, too straight, too weird: Non-reactive
indicators for meaningless data in internet surveys. \emph{Survey Research Methods},
13(3), 229-248.

Greszki, R., Meyer, M., & Schoen, H. (2015). Exploring the effects of removing
"too fast" responses and respondents from web surveys. \emph{Public Opinion
Quarterly}, 79(2), 471-503.
}
\seealso{
\code{\link[=cier_irv]{cier_irv()}} and \code{\link[=cier_mahalanobis]{cier_mahalanobis()}} for response-content indices to
pair with completion time; \code{\link[=cier_methods]{cier_methods()}} for the registry defaults.

Other timing indices:
\code{\link[=cier_page_time]{cier_page_time()}}
}
\concept{timing indices}
