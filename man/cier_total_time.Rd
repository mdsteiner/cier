% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-total-time.R
\name{cier_total_time}
\alias{cier_total_time}
\title{Total survey completion-time C/IER index}
\usage{
cier_total_time(seconds, fpr = NULL, cutoff = NULL, frac_median = NULL)
}
\arguments{
\item{seconds}{A numeric vector of total completion times in seconds, one per
respondent. Must be strictly positive; \code{NA} abstains.}

\item{fpr}{Optional percentile tail mass; \code{NULL} (default) uses \code{0.05}, else a
finite number in \verb{(0, 1)}. Mutually exclusive with \code{cutoff} and \code{frac_median}.}

\item{cutoff}{Optional literal cutoff in seconds, a finite number \verb{>= 0}; flags
respondents at or below it. Mutually exclusive with \code{fpr} and \code{frac_median}.}

\item{frac_median}{Optional median-relative cutoff in \verb{(0, 1]}, resolving to
\code{frac_median * median} of the finite times; flags respondents at or below it.
Mutually exclusive with \code{fpr} and \code{cutoff}.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Flags respondents with the fastest total completion times as likely speeders
(Ward & Meade, 2023; Huang et al., 2012). The value is the completion time in
seconds itself, so low totals flag carelessness (\code{direction = "lower"}); there is
no response matrix.
}
\details{
\code{seconds} is a bare numeric vector of whole-survey durations, one per respondent
(a 2-D input is rejected). Each must be strictly positive (zero/negative is a typed
error; \code{NA} abstains, yielding \code{NA} \code{value}/\code{flagged}). Percentile and
median-relative cutoffs are scale-invariant, but a literal \code{cutoff} is in seconds,
so confirm the export's unit. This index is standalone: \code{\link[=cier_screen]{cier_screen()}} does not
run it.

The default flags the fastest respondents at the lower \code{fpr} quantile (5th
percentile) -- a ranking convention flagging at least \code{fpr} of respondents, not a
calibrated false-positive rate; it needs at least 20 finite times to resolve a
tail. \code{frac_median} instead flags respondents at or below that fraction of the
sample median (Leiner's 2019 RSI uses \code{0.5}; Greszki et al. 2015 use 0.5 / 0.4 /
0.3), robust to up to half the sample responding carelessly. If no respondent has a
finite time the cutoff is \code{NA}, nobody is flagged, and a typed warning is raised.
}
\section{What this catches}{

Respondents who rushed the whole survey -- the canonical speeder profile and the
strongest single signal on fully-careless protocols (Ward & Meade, 2023).
}

\section{What this misses}{

Within-survey speeding bursts (a respondent who races only the second half keeps a
plausible total) and careless respondents who took their time -- completion time is
a floor on effort, not a content check. Biemann et al. (2025) report 46-80\% of
whole-survey straightliners pass a 2 s/item screen, so pair completion time with a
response-content index (\code{\link[=cier_irv]{cier_irv()}} or \code{\link[=cier_mahalanobis]{cier_mahalanobis()}}) and a page-level
timing index when available.
}

\examples{
# A careful bulk of several-minute completions with a fast tail of speeders.
secs <- c(420, 412, 505, 388, 470, 401, 440, 415, 380, 460,
          405, 390, 430, 365, 480, 445, 398, 455, 425, 372,
          95, 47, 88, 110)
cier_total_time(secs)

# Flag respondents faster than half the sample median (Leiner 2019 RSI):
cier_total_time(secs, frac_median = 0.5)
}
\references{
Ward, M. K., & Meade, A. W. (2023). Dealing with careless responding in survey
data. \emph{Annual Review of Psychology}, 74, 577-596.
\doi{10.1146/annurev-psych-040422-045007}

Huang, J. L., Curran, P. G., Keeney, J., Poposki, E. M., & DeShon, R. P.
(2012). Detecting and deterring insufficient effort responding to surveys.
\emph{Journal of Business and Psychology}, 27(1), 99-114.

Leiner, D. J. (2019). Too fast, too straight, too weird: Non-reactive
indicators for meaningless data in internet surveys. \emph{Survey Research Methods},
13(3), 229-248.

Greszki, R., Meyer, M., & Schoen, H. (2015). Exploring the effects of removing
"too fast" responses and respondents from web surveys. \emph{Public Opinion
Quarterly}, 79(2), 471-503.
}
\seealso{
\code{\link[=cier_irv]{cier_irv()}} and \code{\link[=cier_mahalanobis]{cier_mahalanobis()}} for response-content indices to
pair with completion time.

Other timing indices:
\code{\link[=cier_page_time]{cier_page_time()}}
}
\concept{timing indices}
