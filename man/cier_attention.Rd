% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-attention.R
\name{cier_attention}
\alias{cier_attention}
\title{Attention-check C/IER index (Meade & Craig 2012)}
\usage{
cier_attention(checks, pass, cutoff = NULL)
}
\arguments{
\item{checks}{A numeric matrix (data.frame / tibble coerced internally) of raw
attention-check responses, one row per respondent, one column per check. \code{NA}
marks an unanswered check.}

\item{pass}{A length-\code{ncol(checks)} list in column order; \code{pass[[j]]} is a
non-empty numeric vector of the value(s) that pass check \code{j}.}

\item{cutoff}{Optional whole-number failed-check count in \verb{[1, ncol(checks)]};
\code{NULL} (default) flags any failed check.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Counts, per respondent, how many attention checks were failed among those
answered. A check is failed when the answered response is not one of its
declared passing values, so more failures flag carelessness
(\code{direction = "upper"}). Attention checks are items with an author-known
passing response: instructed-response, bogus, and infrequency items.
}
\details{
Any answered response outside \code{pass[[j]]} is a failure. Pass values use the
literal observed-response coding -- no reverse-keying, no \code{items} metadata -- so
checks with different response scales mix freely. An unanswered check (\code{NA}) is
neither pass nor failure; a respondent who answered none abstains (\code{value} and
\code{flagged} are \code{NA}).

This index is standalone: \code{\link[=cier_screen]{cier_screen()}} does not run it, so call it directly.
}
\section{What this catches}{

Random responders, and agreement / extreme straightliners whose constant answer
crosses a check's non-endorsing set.
}

\section{What this misses}{

A careless respondent who answers every check correctly (a straightliner whose
value matches each pass-set, or someone who spots the checks). Pair direct checks
with a response-pattern or timing index.
}

\examples{
# Two checks: a bogus item (pass = 1 or 2) and an instructed item (pass = 0).
checks <- matrix(c(1, 0,
                   3, 0,
                   2, 5,
                   4, 7,
                   1, 0),
                 nrow = 5L, byrow = TRUE)
cier_attention(checks, pass = list(c(1, 2), 0))

# The bundled Bruhlmann data ships two attention checks: bogus (pass 1 or 2)
# and instructed v_IRI (pass 0).
att <- cier_attention(bfi_careless[, c("v_Bogus_Item", "v_IRI")],
                      pass = list(c(1, 2), 0))
sum(att$flagged)   # respondents failing at least one of the two checks
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437-455. \doi{10.1037/a0028085}

Goldammer P., Stöckli, P. L., Escher, Y. A., Annen, H., Jonas, K., &
Antonakis, J. (2024) Careless responding detection revisited: Accuracy of
direct and indirect measures. \emph{Behavior Research Methods}, 56, 8422-8449.
}
\seealso{
\link{bfi_careless} for the bundled bogus and instructed-response check
columns.
}
\concept{direct indices}
