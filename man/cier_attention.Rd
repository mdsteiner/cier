% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-attention.R
\name{cier_attention}
\alias{cier_attention}
\title{Attention-check C/IER index (Meade & Craig 2012)}
\usage{
cier_attention(checks, pass, cutoff = NULL)
}
\arguments{
\item{checks}{A numeric matrix (or a data.frame / tibble coerced internally) of
raw attention-check responses, one row per respondent and one column per
check. \code{NA} marks an unanswered check.}

\item{pass}{A list of length \code{ncol(checks)}, in column order, giving the
passing value(s) of each check: \code{pass[[j]]} is a non-empty numeric vector of
the response value(s) that pass check \code{j}. An answered response outside it is
a failure.}

\item{cutoff}{Optional \strong{literal} failed-check-count cutoff: a single finite
number in \verb{[1, ncol(checks)]}, used verbatim. \code{NULL} (default) uses the
registry default count of \code{1} (any failed check flags).}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Counts, per respondent, how many \strong{attention checks} were failed among those
answered. Attention checks are items with an author-known correct or
non-endorsing response -- instructed-response items ("to show you are reading,
select Strongly Disagree"), bogus items (a nonsensical statement no attentive
respondent endorses), and infrequency items (a statement almost everyone
answers one way). A respondent \strong{fails} a check when their answered response
is \strong{not} one of that check's declared passing values, so \strong{more} failed
checks flag carelessness (\code{direction = "upper"}).
}
\details{
\strong{Input.} \code{checks} is an n x k matrix (a data.frame / tibble is coerced)
holding the raw responses to the k attention-check items, one column per check.
\code{pass} is a list of length k, in the same column order; element \code{j} gives the
value(s) that \strong{pass} check \code{j} -- the directed option for an instructed item
(e.g. \code{0}), the non-endorsing options for a bogus item (e.g. \code{c(1, 2)} on a
1-5 scale), or every value but the infrequent one for an infrequency item. Any
answered response outside \code{pass[[j]]} is a failure.

\strong{No reverse-keying.} The pass values are read in the \strong{literal observed-response
coding}: declare them as the survey platform delivers them. \code{cier_attention()}
applies no reverse-keying and takes no \code{items} metadata. It also makes no
same-scale assumption across checks -- each check has its own pass-set, so
checks with different response scales mix freely.

\strong{Cutoff.} The default flags a respondent with \strong{any} failed check (the cited
Meade & Craig rule: cutoff \code{1}). To require more failures, pass \code{cutoff}, a
literal failed-check count in \verb{[1, k]} (e.g. \code{cutoff = 2} flags only respondents
who failed at least two checks). Respondents whose failed-check count is at or
above the cutoff are flagged.

\strong{Abstention.} An unanswered check (\code{NA}) contributes no evidence: it is
counted as neither a pass nor a failure. A respondent who answered \strong{at least
one} check scores a finite count; a respondent who answered \strong{none} abstains
(\code{value} and \code{flagged} are \code{NA}, and the row is excluded from the flag count and
rate). This is a deliberate choice -- a missing check is treated as absent
information, not as a failure.
}
\section{What this catches}{

Random responders (likely to miss an instructed key or endorse a bogus item);
agreement / extreme straightliners whose constant answer crosses a check's
non-endorsing set; and any respondent inattentive enough to overlook a planted
instruction. Direct checks are the bundle anchor when a survey includes them.
}

\section{What this misses}{

A careless respondent who happens to answer every check correctly (a
straightliner whose single value matches each pass-set; someone who spots and
complies with the checks while answering the rest carelessly). Direct checks are
easy to pass with low effort once noticed, and must be designed into the survey
in advance. Pair them with at least one response-pattern (indirect / pattern)
or timing index.
}

\examples{
# Five respondents, two checks: a bogus item (pass = the non-endorsing 1 or 2)
# and an instructed item (pass = the directed option 0).
checks <- matrix(c(1, 0,
                   3, 0,
                   2, 5,
                   4, 7,
                   1, 0),
                 nrow = 5L, byrow = TRUE)
cier_attention(checks, pass = list(c(1, 2), 0))

# The bundled Bruhlmann data ships two attention checks: a bogus item (passing
# responses 1 or 2) and an instructed item v_IRI (passing response 0).
att <- cier_attention(bfi_careless[, c("v_Bogus_Item", "v_IRI")],
                      pass = list(c(1, 2), 0))
sum(att$flagged)   # respondents failing at least one of the two checks
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437-455. \doi{10.1037/a0028085}

Goldammer P., Stöckli, P. L., Escher, Y. A., Annen, H., Jonas, K., &
Antonakis, J. (2024) Careless responding detection revisited: Accuracy of
direct and indirect measures. \emph{Behavior Research Methods}, 56, 8422-8449.
}
\seealso{
\code{\link[=cier_methods]{cier_methods()}} for the registry defaults; \link{bfi_careless} for the
bundled bogus and instructed-response check columns.
}
\concept{direct indices}
