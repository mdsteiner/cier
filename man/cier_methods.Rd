% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-properties.R
\name{cier_methods}
\alias{cier_methods}
\title{List the detection indices and their registry properties}
\usage{
cier_methods()
}
\value{
A data frame (class \code{cier_method_info}), one row per index, with the
columns listed under Details.
}
\description{
Returns the package's \strong{method registry}: one row per detection index, with
the citable defaults every wrapper reads -- the default cutoff method and
value, the flag direction, the source paper, the optional backend package,
and the screen membership. Use it to see the valid \code{methods} ids for
\code{\link[=cier_screen]{cier_screen()}} and to trace where each index's defaults come from.
}
\details{
The registry is the single source of truth behind the index functions; this
accessor is read-only. Columns:
\itemize{
\item \code{method}: the index function name (also the id for \code{cier_screen(methods=)}).
\item \code{family}: \code{"indirect"} (response-pattern) or \code{"personfit"}.
\item \code{paper_year}, \code{paper_citation_key}, \code{doi}: the source paper the default is
traced to.
\item \code{default_cutoff_method} / \code{default_cutoff_value}: how the default cutoff is
resolved (\code{"percentile"} at a target tail mass, \code{"fixed"} fraction,
\code{"chisq"} tail probability, or \code{"perfit_null"} Monte-Carlo level) and the
cited default rate.
\item \code{flag_direction}: which tail flags carelessness (\code{"upper"} / \code{"lower"}).
\item \code{companion_methods}: indices commonly paired with this one.
\item \code{backend}: the \code{Suggests} package that scores the statistic (\code{NA} for the
pure indices).
\item \code{screenable}: whether \code{\link[=cier_screen]{cier_screen()}} runs it.
\item \code{vote_group}: the construct label the screen collapses correlated indices
under (even-odd and personal reliability share \code{"consistency"}).
\item \code{notes}: a one-line description.
}
}
\examples{
cier_methods()[, c("method", "family", "default_cutoff_method",
                   "flag_direction", "screenable")]
}
\seealso{
\code{\link[=cier_screen]{cier_screen()}}; the index functions, e.g. \code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_irv]{cier_irv()}}, \code{\link[=cier_personal_reliability]{cier_personal_reliability()}}

Other orchestration:
\code{\link[=cier_screen]{cier_screen()}}
}
\concept{orchestration}
