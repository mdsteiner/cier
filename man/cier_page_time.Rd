% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-page-time.R
\name{cier_page_time}
\alias{cier_page_time}
\title{Per-page rapid-responding C/IER index (Bowling et al. 2023)}
\usage{
cier_page_time(
  page_seconds,
  items_per_page,
  min_seconds = 2,
  frac = NULL,
  cutoff = NULL
)
}
\arguments{
\item{page_seconds}{A numeric matrix (or a data.frame / tibble coerced
internally) of per-page total times in seconds, one row per respondent and
one column per page. Must be strictly positive; \code{NA} marks an untimed page.}

\item{items_per_page}{A vector of positive whole numbers, length
\code{ncol(page_seconds)}, giving the number of items on each page (in column
order).}

\item{min_seconds}{Single finite number \verb{>= 0}. The per-item time threshold in
seconds; a page whose mean per-item time is strictly below it is counted
rapid. Default \code{2} per Bowling et al. (2023).}

\item{frac}{Optional cutoff as a \strong{fraction} of the page count: a single
finite number in \verb{(0, 1]}, resolving to \code{ceiling(frac * pages)}. Mutually
exclusive with \code{cutoff}. \code{NULL} (default) uses the registry default count
of \code{1}.}

\item{cutoff}{Optional \strong{literal} rapid-page-count cutoff: a single finite
number in \verb{[1, pages]}, used verbatim. Supplied instead of \code{frac}.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Counts, per respondent, the number of survey pages answered too fast to be
plausible -- pages whose \strong{mean per-item time} falls strictly below
\code{min_seconds} (default \code{2} seconds per item, following Bowling, Huang, Brower,
& Bragg, 2023). The mean per-item time on a page is the respondent's \strong{total}
time on that page divided by the number of items on it, so \strong{more} rapid
pages flag carelessness (\code{direction = "upper"}).
}
\details{
\strong{Input.} \code{page_seconds} is an n x pages matrix (a data.frame / tibble is
coerced) holding each respondent's \strong{total} time on each page, one column per
page -- the shape survey platforms export (e.g. a Qualtrics page-submit timer).
\code{items_per_page} gives the number of items on each page, in the same column
order; the page total is divided by it to get the per-item rate. (If your
input is already a per-item rate, pass \code{items_per_page = rep(1, ncol)}.) Each
observed page time must be strictly positive; a zero or negative duration is a
typed error, and a missing time (\code{NA}) marks an untimed page that contributes
no evidence.

\strong{Cutoff.} The default flags a respondent with \strong{any} rapid page (the cited
Bowling et al. rule: cutoff \code{1}). On a long survey a single rapid page can be
an over-sensitive trigger, so override the count with \strong{one} of two mutually
exclusive arguments: \code{frac}, a fraction of the \strong{total page count} in
\verb{(0, 1]} resolving to \code{ceiling(frac * pages)} (e.g. \code{frac = 0.1} on 40 pages
requires four rapid pages), or \code{cutoff}, a literal rapid-page count in
\verb{[1, pages]}. Respondents whose rapid-page count is at or above the resolved
cutoff are flagged.

\strong{Abstention.} A page with no recorded time (\code{NA}) contributes no evidence:
it is neither counted rapid nor counted toward the page total, and the per-item
denominator is the declared \code{items_per_page}, not a count of answered cells. A
respondent for whom \strong{every} page is untimed abstains: both \code{value} and
\code{flagged} are \code{NA} and the row is excluded from the flag count and rate.

\strong{This is an absolute-threshold heuristic by design.} The \code{2} s/item floor is
a cited fixed rule, not an empirical percentile -- a respondent below it is
called rapid regardless of the rest of the sample. Page time is a \emph{floor} on
effort, not a content check.
}
\section{What this catches}{

Within-survey speeding bursts that the total-time index misses -- a respondent
who attends to the first part of the survey but races one or more later pages
registers those pages as rapid even when the whole-survey total looks plausible.
}

\section{What this misses}{

Slow careless responding (a respondent who clicks deliberately but
inattentively never trips the rapid-page threshold). Pair it with a
response-content index from the indirect or pattern families, and with
\code{\link[=cier_total_time]{cier_total_time()}} for the whole-survey floor.
}

\examples{
# Five respondents, three two-item pages, per-page total times in seconds.
page_seconds <- matrix(c(20, 18, 22,
                         19, 21, 20,
                          3,  2, 24,
                         22, 20, 19,
                          2,  2,  2),
                       nrow = 5L, byrow = TRUE)
cier_page_time(page_seconds, items_per_page = c(2, 2, 2))

# Flag only respondents with at least half their pages rapid (here 2 of 3),
# rather than any single rapid page:
cier_page_time(page_seconds, items_per_page = c(2, 2, 2), frac = 0.5)
}
\references{
Bowling, N. A., Huang, J. L., Brower, C. K., & Bragg, C. B. (2023). The quick
and the careless: The construct validity of page time as a measure of
insufficient effort responding to surveys. \emph{Organizational Research Methods},
26(2), 323-352. \doi{10.1177/10944281211056520}

Huang, J. L., Curran, P. G., Keeney, J., Poposki, E. M., & DeShon, R. P.
(2012). Detecting and deterring insufficient effort responding to surveys.
\emph{Journal of Business and Psychology}, 27(1), 99-114.
}
\seealso{
\code{\link[=cier_total_time]{cier_total_time()}} for the whole-survey timing floor;
\code{\link[=cier_methods]{cier_methods()}} for the registry defaults.

Other timing indices:
\code{\link[=cier_total_time]{cier_total_time()}}
}
\concept{timing indices}
