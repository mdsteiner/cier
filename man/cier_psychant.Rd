% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-psychant.R
\name{cier_psychant}
\alias{cier_psychant}
\title{Psychometric antonyms (within-person antonym opposition) C/IER index}
\usage{
cier_psychant(responses, critical_r = 0.6, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{critical_r}{The whole-sample inter-item correlation magnitude a pair must
exceed (on the negative tail) to count as an antonym. A single \strong{positive}
number in the open interval \verb{(0, 1)}; the default \code{0.60} follows Meade &
Craig (2012) and keeps pairs whose inter-item correlation is below \code{-0.60}.
(Unlike \code{careless::psychant()}, which takes a negative \code{critval}.)}

\item{fpr}{Optional target false-positive tail mass for the percentile cutoff.
\code{NULL} (default) uses the registry default \code{0.05}. A finite number in the
open interval \verb{(0, 1)}; the cutoff is that upper-tail quantile of the
observed scores. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the correlation, supplied instead
of \code{fpr}. A single finite number in \verb{[-1, 1]}; respondents whose antonym
correlation is at or above it are flagged (the upper-tail direction).}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{psychometric-antonyms} score -- the Pearson
correlation, within that respondent, across the item pairs that are strongly
\emph{negatively} correlated in the whole sample (the "antonyms"). A careful
respondent answers genuine antonyms (for instance a trait item and its
reverse-keyed twin) in opposition, so their within-person pattern mirrors the
sample's and the correlation stays strongly negative; a careless respondent's
answers to antonyms decouple, driving the correlation up toward zero. \strong{High}
values therefore flag carelessness -- the mirror of \code{\link[=cier_psychsyn]{cier_psychsyn()}}, whose
low values flag (Meade & Craig, 2012).
}
\details{
\strong{Pairing on raw responses.} The antonym pairs are the item pairs whose
whole-sample inter-item correlation is below \code{-critical_r} (more negative than
\code{-0.60} by default). Pairing uses the \strong{raw} responses, with no reverse-
scoring: reverse-keying would collapse the very negative correlations that
define an antonym pair toward zero. The score is the single correlation across
all qualifying pairs' stacked values, matching careless's antonym scorer
(\code{careless::psychsyn(anto = TRUE, resample_na = FALSE)}) to 1e-12.

\strong{\code{critical_r} is a positive magnitude.} Supply \code{critical_r} as a \strong{positive}
number in \verb{(0, 1)} (default \code{0.60}); the index applies it to the negative tail
internally, keeping pairs whose inter-item correlation is below \code{-critical_r}.
This matches
\code{\link[=cier_psychsyn]{cier_psychsyn()}} and the pairing diagnostics
(\code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}}, \code{\link[=cier_psychsyn_critval]{cier_psychsyn_critval()}} with \code{antonym = TRUE}), so a
single positive threshold means the same pair strength for synonyms and
antonyms alike. Note that \code{careless::psychant()} instead takes a \strong{negative}
\code{critval} (e.g. \code{-0.60}); here pass the positive magnitude \code{0.60}. A negative
\code{critical_r} is rejected as an input error.

\strong{No pairs found.} \code{critical_r} is a property of the \emph{inventory}, not the
respondent: a survey may contain no two items that oppose each other strongly
enough, so no antonym pairs qualify, every value is \code{NA}, and the index
abstains with a warning. Lower \code{critical_r} to surface pairs, or compute the
index on a scale that pairs forward and reverse items. Use
\code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}} (\code{antonym = TRUE}) to see which pairs qualify.

\strong{Cutoff.} The default flags the lowest-opposition respondents: the cutoff is
the empirical \code{1 - fpr} quantile of the observed scores (the 95th percentile by
default) and respondents at or above it are flagged. This is a \strong{ranking}
convention -- a sample percentile flags \code{fpr} of respondents by construction --
not a calibrated false-positive rate. Adjust the target tail with \code{fpr}, or
pass an absolute \code{cutoff} in \verb{[-1, 1]} to flag on a literal correlation
threshold. \code{fpr} and \code{cutoff} are mutually exclusive.

\strong{Abstention.} A respondent with fewer than three complete antonym pairs (an
all-missing row included), or whose answered pair values have zero variance (a
straightliner), has no defined correlation: both \code{value} and \code{flagged} are \code{NA}
and the row is excluded from the flag count and rate.
}
\section{What this catches}{

Random and partial / changepoint responding, whose answers to opposed items
lose the sample's consistency. It \strong{misses} pure straightlining and midpoint
locking (a constant row has zero variance and abstains); pair it with
\code{\link[=cier_longstring]{cier_longstring()}} and \code{\link[=cier_irv]{cier_irv()}}. It also needs genuine antonym pairs to
exist in the inventory -- forward/reverse item pairs are the usual source.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data. A
# broad personality inventory's strongest item oppositions are milder than the
# default critical_r of 0.60 (the BFI's most negative inter-item r is about
# -0.48), so lower it to surface antonym pairs -- typically forward/reverse
# item pairs within a scale.
out <- cier_psychant(bfi_careless[, 1:44], critical_r = 0.40)
out
head(as.data.frame(out))
}
\references{
Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437–455. \doi{10.1037/a0028085}
}
\seealso{
\code{\link[careless:psychant]{careless::psychant()}}, \code{\link[=cier_psychsyn]{cier_psychsyn()}}, \code{\link[=cier_synonym_pairs]{cier_synonym_pairs()}},
\code{\link[=cier_irv]{cier_irv()}}

Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
