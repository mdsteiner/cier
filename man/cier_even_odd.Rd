% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-even-odd.R
\name{cier_even_odd}
\alias{cier_even_odd}
\title{Even-odd consistency C/IER index}
\usage{
cier_even_odd(responses, items, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{items}{A data.frame of item metadata, one row per item, aligned to the
columns of \code{responses}. Must carry a \code{scale} column with at least two
distinct labels. Optional \code{reverse_keyed} (logical, default none) marks
reverse-keyed items; when any item is reverse-keyed an integer \code{max} column
(the largest response option, at least \code{min + 1}) is required. Optional
integer \code{min} gives the scale base (default \code{1}). When \code{responses} has
column names, an optional \code{item} column (or matching row names) is
cross-checked against them so a reordered frame is a typed error.}

\item{fpr}{Optional tail mass in \verb{(0, 1)} for the upper-tail (\code{1 - fpr}) quantile
cutoff; \code{NULL} (default) uses \code{0.05}. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional literal cutoff in \verb{[-1, 1]}; respondents at or above it are
flagged.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's even-odd consistency. Within each scale block (given
by \code{items$scale}) the even-position and odd-position item means are formed,
correlated across scale blocks, Spearman-Brown corrected (\verb{2r / (1 + r)}, clamped
at \code{-1}), and negated. A careful respondent's halves correlate highly (low
negated score); a careless respondent's decouple, so high values flag
carelessness.
}
\details{
Reverse-keyed items (\code{items$reverse_keyed}) are reverse-scored with \code{(min + max) - x}
before the half-means, so the index sees trait-aligned responses. \code{min} defaults to
\code{1}; declare \code{items$min} for a 0-based or bipolar scale.

The default cutoff flags the upper \code{1 - fpr} quantile (95th percentile) -- a ranking
convention flagging at least \code{fpr} of respondents, not a calibrated false-positive
rate. The Spearman-Brown clamp maps every across-block correlation \verb{<= -1/3} to \code{+1},
so the negated score has a point mass at \code{+1}; a percentile cutoff landing on that
atom flags more than \code{fpr}.

Abstention (\code{value} and \code{flagged} are \code{NA}): fewer than two scorable scale blocks, or
half-mean vectors with no across-scale variance (a straightliner row abstains only if
reverse-scoring leaves it constant). With exactly two scorable blocks the correlation
is \code{+1}/\code{-1} by construction -- a degenerate binary score returned with a typed
warning; \verb{>= 3} multi-item scales are recommended.
}
\section{What this catches}{

Random, alternating, and opposite-pattern responding whose within-scale halves
diverge. It misses pure straightlining and midpoint locking (the constant row
abstains) -- catch those with \code{\link[=cier_longstring]{cier_longstring()}} and \code{\link[=cier_irv]{cier_irv()}}.
}

\examples{
# Item metadata from BFI-44 column names: scale = letter run after "v_BFI_",
# trailing "_R" = reverse-keyed, items coded 1..5.
nm <- names(bfi_careless)[1:44]
items <- data.frame(
  scale = sub("^v_BFI_([A-Za-z]+)[0-9].*$", "\\\\1", nm),
  reverse_keyed = grepl("_R$", nm),
  max = 5L
)
# Many respondents tie at the +1 clamp maximum, so the default 95th-percentile
# cutoff lands on it and flags >5\% -- an expected `cier_warning_saturated_cutoff`.
# Set an explicit `cutoff` to avoid it.
out <- cier_even_odd(bfi_careless[, 1:44], items)
out
head(as.data.frame(out))
}
\references{
Curran, P. G. (2016). Methods for the detection of carelessly invalid
responses in survey data. \emph{Journal of Experimental Social Psychology}, 66,
4–19. \doi{10.1016/j.jesp.2015.07.006}

Jackson, D. N. (1976). The appraisal of personal reliability. Paper presented
at the meeting of the Society of Multivariate Experimental Psychology,
University Park, PA.
}
\seealso{
\code{\link[=cier_longstring]{cier_longstring()}}, \code{\link[=cier_irv]{cier_irv()}}

Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
