% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-even-odd.R
\name{cier_even_odd}
\alias{cier_even_odd}
\title{Even-odd consistency C/IER index}
\usage{
cier_even_odd(responses, items, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{items}{A data.frame of item metadata, one row per item, aligned to the
columns of \code{responses}. Must carry a \code{scale} column with at least two
distinct scale labels. An optional logical \code{reverse_keyed} column marks
reverse-keyed items (default: none); when any item is reverse-keyed an
integer \code{categories} column (the number of response options, \verb{>= 2}) is
required so those items can be reverse-scored. An optional integer \code{min}
column gives the smallest response option (the scale base; default \code{1}, i.e.
\verb{1..categories} coding) -- declare it for 0-based or bipolar scales.}

\item{fpr}{Optional target false-positive tail mass for the percentile
cutoff. \code{NULL} (default) uses the registry default \code{0.05}. A finite number
in the open interval \verb{(0, 1)}; the cutoff is that upper-tail (\code{1 - fpr})
quantile of the observed scores. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the score, supplied instead of
\code{fpr}. A single finite number in \verb{[-1, 1]}; respondents whose even-odd score
is at or above it are flagged (the upper-tail direction). Use it to apply an
absolute threshold rather than a sample percentile.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{even-odd consistency}. Within each scale block
(given by \code{items$scale}) the mean of the even-positioned items and the mean of
the odd-positioned items are formed; the two are correlated across scale
blocks, Spearman-Brown corrected (\verb{2r / (1 + r)}, clamped at \code{-1}), and
\strong{negated}. A careful respondent answers the two halves of each scale
consistently, so the halves correlate highly and the negated score is low; a
careless respondent's halves decouple, so the score is high. \strong{High} values
therefore flag carelessness.
}
\details{
\strong{Reverse-keying.} Reverse-keyed items (\code{items$reverse_keyed}) are
reverse-scored with the self-inverse reflection \code{(min + max) - x} (where
\code{max = min + categories - 1}) \emph{before} the half-means are formed, so the index
always sees trait-aligned responses. The scale base \code{min} defaults to \code{1} (the
\verb{1..categories} coding), giving the classic \code{(categories + 1) - x}; declare
\code{items$min} for a 0-based or bipolar scale so reverse items reflect onto the
same range. Supply the raw responses and declare reverse items through \code{items};
pre-scoring them and leaving \code{reverse_keyed = FALSE} yields the same result.

\strong{Cutoff.} The default flags the highest-inconsistency respondents: the cutoff
is the empirical \code{1 - fpr} quantile of the observed scores (the 95th percentile
by default) and respondents at or above it are flagged. This is a \strong{ranking}
convention -- a sample percentile flags \code{fpr} of respondents by construction --
not a calibrated false-positive rate. Adjust the target tail with \code{fpr}, or
pass an absolute \code{cutoff} in \verb{[-1, 1]} to flag on a literal score threshold
(e.g. one carried over from a calibration sample). \code{fpr} and \code{cutoff} are
mutually exclusive.

\strong{Abstention.} A respondent for whom fewer than two scale blocks yield a
finite even-and-odd mean pair, or whose (reverse-scored) half-mean vectors have
no variance across scales, has no defined consistency correlation: both \code{value}
and \code{flagged} are \code{NA} and the row is excluded from the flag count and rate. A
scale with only one item cannot be split and is skipped. Note that a constant
(raw straightliner) row abstains only when reverse-scoring leaves it constant --
a forward-keyed battery, or a value at the scale midpoint. With reverse-keyed
items an off-midpoint constant row is reflected into a non-constant one and so
receives a finite score rather than abstaining.
}
\section{What this catches}{

Random, alternating, and opposite-pattern responding whose within-scale halves
diverge. On a forward-keyed (or scale-midpoint) battery it \strong{misses} pure
straightlining and midpoint locking -- the constant row's even and odd means
are identical, so it abstains. With reverse-keyed items an off-midpoint
straightliner is instead reflected into a non-constant row and scored, but the
score is not a reliable careless signal, so straightlining is still best caught
directly with \code{\link[=cier_longstring]{cier_longstring()}} and \code{\link[=cier_irv]{cier_irv()}}.
}

\examples{
# Build item metadata from the BFI-44 column names: the scale is the letter
# run after "v_BFI_", a trailing "_R" marks a reverse-keyed item, 5 options.
nm <- names(bfi_careless)[1:44]
items <- data.frame(
  scale = sub("^v_BFI_([A-Za-z]+)[0-9].*$", "\\\\1", nm),
  reverse_keyed = grepl("_R$", nm),
  categories = 5L
)
out <- cier_even_odd(bfi_careless[, 1:44], items)
out
head(as.data.frame(out))
}
\references{
Curran, P. G. (2016). Methods for the detection of carelessly invalid
responses in survey data. \emph{Journal of Experimental Social Psychology}, 66,
4–19. \doi{10.1016/j.jesp.2015.07.006}

Jackson, D. N. (1976). The appraisal of personal reliability. Paper presented
at the meeting of the Society of Multivariate Experimental Psychology,
University Park, PA.
}
\seealso{
\code{\link[careless:evenodd]{careless::evenodd()}}, \code{\link[=cier_longstring]{cier_longstring()}}, \code{\link[=cier_irv]{cier_irv()}}

Other indirect indices:
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
