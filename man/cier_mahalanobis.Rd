% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-mahalanobis.R
\name{cier_mahalanobis}
\alias{cier_mahalanobis}
\title{Mahalanobis-distance C/IER index}
\usage{
cier_mahalanobis(responses, alpha = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{alpha}{Optional chi-square upper-tail probability; \code{NULL} (default) uses
\code{0.001}, else a number in \verb{(0, 1)} giving cutoff \code{qchisq(1 - alpha, df = p)}.
Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional literal cutoff on D², a finite number \verb{>= 0}; respondents
at or above it are flagged.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's squared Mahalanobis distance D² of their response
vector from the sample centroid (Curran, 2016), with covariance estimated
pairwise. A profile far from the multivariate centre has a large D², so high
values flag carelessness.
}
\details{
\strong{Cutoff.} D² is referred to a chi-square reference (a real null, not a sample
percentile) with \code{p} degrees of freedom (\code{p} = number of items): the cutoff is
\code{qchisq(1 - alpha, df = p)} with \code{alpha = 0.001} (Tabachnick & Fidell's
conservative \code{p < .001} criterion). The flag rate is therefore not pinned at
\code{alpha} and can run higher under contamination. Override with one of \code{alpha}
(chi-square tail probability) or \code{cutoff} (literal threshold on D²); \code{df = p}
regardless.

\strong{Partial rows.} A respondent's missing items are mean-imputed so the row is
scored, but the observed terms are still weighted by the full inverse covariance
and referred to the same \verb{χ²(p)}, which over-flags partial rows under inter-item
correlation. When any scored row carries a mean-imputed item the index warns
(\code{cier_warning_partial_rows}); re-run on complete cases as a sensitivity check.

\strong{Abstention.} An all-\code{NA} respondent abstains (\code{value} and \code{flagged} are \code{NA}).
If no distance is computable for anyone -- fewer than two respondents carry data,
singular covariance, or pairwise covariance indefinite under heavy/structured
missingness -- every \code{value} is \code{NA}, a warning is emitted, and no one is flagged.
}
\section{What this catches}{

Multivariate outliers: response profiles inconsistent with the sample's covariance
structure, including some random and patterned responding. Being relative to this
sample's centroid, a largely careless sample shifts the centroid and weakens it.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
out <- cier_mahalanobis(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Curran, P. G. (2016). Methods for the detection of carelessly invalid
responses in survey data. \emph{Journal of Experimental Social Psychology}, 66,
4–19. \doi{10.1016/j.jesp.2015.07.006}

Tabachnick, B. G., & Fidell, L. S. (2014). \emph{Using Multivariate Statistics}
(6th ed.). Pearson New International Edition. (Conservative \code{p < .001}
criterion for multivariate outliers via Mahalanobis distance.)
}
\seealso{
Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
