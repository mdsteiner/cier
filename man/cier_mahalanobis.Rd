% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-mahalanobis.R
\name{cier_mahalanobis}
\alias{cier_mahalanobis}
\title{Mahalanobis-distance C/IER index}
\usage{
cier_mahalanobis(responses, alpha = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{alpha}{Optional chi-square upper-tail probability for the cutoff. \code{NULL}
(default) uses the registry default \code{0.001}. A single finite number in
\verb{(0, 1)}; the cutoff is \code{qchisq(1 - alpha, df = p)}. Mutually exclusive with
\code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the squared distance, supplied
instead of \code{alpha}. A single finite number \verb{>= 0}; respondents whose D² is
at or above it are flagged.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{squared Mahalanobis distance} D² of their
response vector from the sample centroid (Curran, 2016). A response profile
far from the multivariate centre -- an inconsistent or atypical pattern across
items -- has a large D², so \strong{high} values flag carelessness. The covariance
is estimated \strong{pairwise}, and a respondent's missing items drop out of the
distance rather than discarding the whole row.
}
\details{
\strong{Cutoff.} The default uses a \strong{proper reference distribution} rather than a
sample percentile: D² is referred to a chi-square distribution with \code{p}
degrees of freedom (\code{p} = the number of items), and respondents beyond the
upper tail are flagged -- the cutoff is \code{qchisq(1 - alpha, df = p)} with
\code{alpha = 0.001}. Because this is a real null (not a ranking convention), the
flag rate is \strong{not} pinned at \code{alpha}: it is whatever share of respondents
exceed the χ²(\code{p}) quantile, and can be informatively higher under
contamination. The \code{0.001} default follows Tabachnick and Fidell's (2019)
recommendation of a conservative \code{p < .001} criterion for identifying
multivariate outliers by Mahalanobis distance. It is \strong{stricter} than
\code{careless::mahad()}, whose default flags the upper 1\% tail (its
\code{confidence = 0.99}, i.e. \code{alpha = 0.01}); pass \code{alpha = 0.01} to reproduce
the \code{careless} default. Override the cutoff with \strong{one} of two mutually
exclusive arguments: \code{alpha} (the chi-square tail probability) or \code{cutoff}
(a literal threshold on D²).

\strong{Abstention.} A respondent who answered nothing (an all-\code{NA} row) abstains:
both \code{value} and \code{flagged} are \code{NA}. If \strong{no} distance can be computed for
anyone -- fewer than two respondents carry any data, or the covariance matrix
is singular (for example more items than respondents, or perfectly collinear
items) -- every \code{value} is \code{NA}, a warning is emitted, and no one is flagged.
}
\section{What this catches}{

Multivariate outliers: response profiles inconsistent with the sample's
covariance structure, including some random and patterned responding. It is a
relative measure (distance from \emph{this} sample's centroid), so a sample that is
largely careless shifts the centroid and weakens it.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
out <- cier_mahalanobis(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Curran, P. G. (2016). Methods for the detection of carelessly invalid
responses in survey data. \emph{Journal of Experimental Social Psychology}, 66,
4–19. \doi{10.1016/j.jesp.2015.07.006}

Tabachnick, B. G., & Fidell, L. S. (2019). \emph{Using Multivariate Statistics}
(7th ed.). Pearson. (Conservative \code{p < .001} criterion for multivariate
outliers via Mahalanobis distance.)
}
\seealso{
\code{\link[careless:mahad]{careless::mahad()}}, \code{\link[psych:outlier]{psych::outlier()}}

Other indirect indices:
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_person_total]{cier_person_total()}}
}
\concept{indirect indices}
