% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-autocorrelation.R
\name{cier_autocorrelation}
\alias{cier_autocorrelation}
\title{Autocorrelation C/IER index}
\usage{
cier_autocorrelation(
  responses,
  min_lag = 1L,
  max_lag = NULL,
  na_rm = FALSE,
  fpr = NULL,
  cutoff = NULL
)
}
\arguments{
\item{responses}{Numeric matrix (or data.frame / tibble coerced internally), one
row per respondent, one column per item, in administration order; \code{NA} marks a
missing response.}

\item{min_lag}{Positive whole number; smallest lag to evaluate. Default \code{1}.}

\item{max_lag}{Positive whole number or \code{NULL} (default, resolves to
\code{min(ncol(responses) - 3, 10)}); largest lag to evaluate. An explicit value must
satisfy \verb{min_lag <= max_lag <= ncol(responses) - 3}.}

\item{na_rm}{Single \code{TRUE} / \code{FALSE}. \code{TRUE} strips each row's missing responses
before lagging; \code{FALSE} (default) handles missingness pairwise within each lag.}

\item{fpr}{Optional false-positive tail mass for the percentile cutoff; finite
number in \verb{(0, 1)}, \code{NULL} (default) uses \code{0.05}. Mutually exclusive with
\code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the autocorrelation magnitude, in
\verb{[0, 1]}, supplied instead of \code{fpr}; scores at or above it are flagged.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's maximum absolute lag autocorrelation -- the largest
absolute Pearson correlation between their raw response row (administration order,
no reverse-keying or scale blocking) and its lag-shifted self over a range of lags
(Gottfried, Jezek, Kralova & Rihacek, 2022). A predictable repeating pattern
(seesaw, diagonal, straightlining) yields high autocorrelation, so high values
flag carelessness.
}
\details{
\strong{Cutoff.} The default flags the upper \code{fpr} quantile of observed scores (95th
percentile), the paper's relative-ranking recommendation; or pass an absolute
\code{cutoff} in \verb{[0, 1]} instead.

\strong{Zero-variance convention.} A constant lag slice (a straightliner) has an
undefined correlation and scores \code{1} rather than abstaining, so it is flagged.

\strong{Missing data.} With \code{na_rm = FALSE} (default) missingness is pairwise within
each lag; a lag with fewer than three complete pairs abstains (a two-pair
correlation is \verb{+/-1} by construction), and a row with too few scorable lags
abstains entirely (\code{value} and \code{flagged} \code{NA}). \code{na_rm = TRUE} strips each row's
missing responses before lagging, collapsing administration-order spacing, and is
rarely appropriate.

\strong{Assumptions.} Columns are read as an ordered sequence on a common response
scale, so the index is meaningful only when they are in administration order with
the same number of options; score each same-format block separately on a
mixed-format survey.

Standalone: \code{\link[=cier_screen]{cier_screen()}} does not run it, so call it directly.
}
\section{What this catches}{

Repetitive / periodic careless responding -- seesaw, alternating, diagonal, and
(via the zero-variance convention) straightlining. Not useful against random
responding, whose autocorrelation is near zero.
}

\examples{
# First 44 columns are the BFI items; max_lag defaults to min(ncol - 3, 10) = 10.
out <- cier_autocorrelation(bfi_careless[, 1:44])
out
head(as.data.frame(out))
# Restrict to a handful of low lags:
cier_autocorrelation(bfi_careless[, 1:44], max_lag = 6L)
}
\references{
Gottfried, J., Jezek, S., Kralova, M., & Rihacek, T. (2022). Autocorrelation
screening: A potentially efficient method for detecting repetitive response
patterns in questionnaire data. \emph{Practical Assessment, Research & Evaluation},
27, Article 2. \doi{10.7275/vyxb-gt24}
}
\seealso{
Other indirect indices:
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
