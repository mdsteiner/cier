% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-autocorrelation.R
\name{cier_autocorrelation}
\alias{cier_autocorrelation}
\title{Autocorrelation C/IER index}
\usage{
cier_autocorrelation(
  responses,
  min_lag = 1L,
  max_lag = NULL,
  na_rm = FALSE,
  fpr = NULL,
  cutoff = NULL
)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item, in
administration order. \code{NA} marks a missing response.}

\item{min_lag}{Positive whole number. The smallest lag to evaluate. Default
\code{1}.}

\item{max_lag}{Positive whole number or \code{NULL}. The largest lag to evaluate.
\code{NULL} (default) resolves to \code{min(ncol(responses) - 3, 10)} -- capped at the
low lags Gottfried et al. recommend, since the full \code{ncol(responses) - 3}
range saturates short high-lag slices to 1 on a long battery. An explicit
value must satisfy \verb{min_lag <= max_lag <= ncol(responses) - 3} (the cap
applies to the default only). The default is sized from the full column
count even when \code{na_rm = TRUE}, so a heavily missing row may have its higher
lags abstain once its responses are stripped.}

\item{na_rm}{Single \code{TRUE} / \code{FALSE}. When \code{TRUE}, each row's missing
responses are stripped before lagging; when \code{FALSE} (default) missingness is
handled pairwise within each lag.}

\item{fpr}{Optional target false-positive tail mass for the percentile cutoff.
\code{NULL} (default) uses the registry default \code{0.05}. A finite number in the
open interval \verb{(0, 1)}; the cutoff is that upper-tail quantile of the
observed scores. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the autocorrelation magnitude,
supplied instead of \code{fpr}. A single finite number in \verb{[0, 1]}; respondents
whose score is at or above it are flagged.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{maximum absolute lag autocorrelation} -- the
largest absolute Pearson correlation between their response vector and its
lag-shifted self over a range of lags (Gottfried, Jezek, Kralova & Rihacek,
2022), as packaged in \code{responsePatterns::rp.acors()}. A respondent whose
answers follow a predictable repeating pattern (seesaw / alternating,
diagonal, or straightlining) has a high autocorrelation, so \strong{high} values
flag carelessness. The score is computed over the \strong{raw} response row in
administration order -- there is no reverse-keying and no scale blocking.
}
\details{
\strong{Cutoff.} The default flags the most autocorrelated respondents: the cutoff
is the empirical upper \code{fpr} quantile of the observed scores (the 95th
percentile by default) and respondents at or above it are flagged -- \strong{at
least} \code{fpr} of them, more when scores tie at the cutoff. This
empirical-ranking convention is the paper's own recommendation ("rely on
relative, not absolute criteria"). Adjust the target tail with \code{fpr}, or pass
an absolute \code{cutoff} in \verb{[0, 1]} to flag on a literal autocorrelation
magnitude; \code{fpr} and \code{cutoff} are mutually exclusive.

\strong{Lag range.} \code{max_lag = NULL} resolves to \code{min(ncol(responses) - 3, 10)} --
the low lags Gottfried et al. recommend (roughly 5-12 on runs of 10-40 items),
capped at 10. The full \code{ncol(responses) - 3} range (the \code{rp.acors()} default:
the largest lag whose slice still holds three observations) adds many short,
high-lag slices whose correlation saturates near 1 on a long battery, flagging
far more than the target rate; the cap keeps the default in the recommended
band. An explicit \code{max_lag} may still reach \code{ncol(responses) - 3} (the cap is
default-only), so set \code{max_lag} and \code{min_lag} deliberately for a long battery.

\strong{Zero-variance convention.} A lag whose slice is constant (a straightliner)
has an undefined correlation; following \code{rp.acors()} it scores \code{1} -- the top
of the tail -- rather than abstaining. This differs from \code{stats::acf()} (which
returns \code{NaN}). A straightliner is therefore \strong{flagged}, not dropped.

\strong{Missing data.} With \code{na_rm = FALSE} (default) missingness is handled
pairwise within each lag (a missing cell drops only the lagged pairs it
touches). A lag whose pairwise-complete subset holds fewer than three pairs
abstains for that lag -- a two-pair correlation is \verb{+/-1} by construction, so
without this guard an early dropout would be deterministically flagged. Heavy
per-row missingness therefore inflates the few surviving correlations toward 1
and, once too few lags remain scorable, the respondent abstains entirely (both
\code{value} and \code{flagged} are \code{NA}, and the row is excluded from the flag count and
rate). Treat scores from rows with many missing items cautiously; a
minimum-answered-items screen before this index is recommended. With
\code{na_rm = TRUE} each row's missing responses are removed before lagging; this
collapses the administration-order spacing the index depends on and is rarely
appropriate.

\strong{Assumptions.} The index reads the response columns as an ordered sequence
on a common response scale, so it is meaningful only when the columns are in
administration order and share the same number of response options. This is
the source paper's own scale-format guidance: "only questions with the same
answer scales should be analyzed at one time", since mixing scales "with
vastly different number ranges ... can bias the results to a great extent",
and questions with unique scales or answer options (say gender or education)
"should be excluded prior to screening" (Gottfried et al., 2022). On a
mixed-format survey, score each same-format block in a separate call.
}
\section{What this catches}{

Repetitive / periodic careless responding -- seesaw, alternating, diagonal,
and (via the zero-variance convention) straightlining -- whose lagged response
is predictable from the previous. It is \strong{not} useful against random
responding, whose autocorrelation is near zero.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
# max_lag defaults to min(ncol - 3, 10) = 10 here -- the low-lag range
# Gottfried et al. recommend; the full ncol - 3 = 41 would add short high-lag
# slices that saturate near 1, flagging far more than the target rate.
out <- cier_autocorrelation(bfi_careless[, 1:44])
out
head(as.data.frame(out))
# Restrict further to a handful of low lags:
cier_autocorrelation(bfi_careless[, 1:44], max_lag = 6L)
}
\references{
Gottfried, J., Jezek, S., Kralova, M., & Rihacek, T. (2022). Autocorrelation
screening: A potentially efficient method for detecting repetitive response
patterns in questionnaire data. \emph{Practical Assessment, Research & Evaluation},
27, Article 2. \doi{10.7275/vyxb-gt24}
}
\seealso{
\code{\link[responsePatterns:rp.acors]{responsePatterns::rp.acors()}}

Other indirect indices:
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
