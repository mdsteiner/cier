% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-irv.R
\name{cier_irv}
\alias{cier_irv}
\title{Intra-individual response variability (IRV) C/IER index}
\usage{
cier_irv(responses, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{fpr}{Optional lower-tail mass in \verb{(0, 1)} for the percentile cutoff; \code{NULL}
(default) uses \code{0.05}. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional literal IRV cutoff; respondents at or below it are flagged.
Mutually exclusive with \code{fpr}.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Each respondent's IRV is the sample SD of their answers across items
(Marjanovic et al., 2015), computed over the raw response row (no scale
blocking). Low values flag carelessness.
}
\details{
The default flags the lowest-variability respondents at the \code{fpr} quantile (5th
percentile), a ranking convention that flags at least \code{fpr} of respondents, not a
calibrated false-positive rate. Blank items are dropped; a respondent with fewer
than two answered items has no defined SD (\code{value} and \code{flagged} are \code{NA}), while a
constant respondent with at least two items scores \code{0} and is flagged.
}
\section{What this catches}{

Straightlining and midpoint responding, which compress the response range. Not
useful against random responding, which inflates variability.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
out <- cier_irv(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Marjanovic, Z., Holden, R., Struthers, W., Cribbie, R., & Greenglass, E.
(2015). The inter-item standard deviation (ISD): An index that discriminates
between conscientious and random responders. \emph{Personality and Individual
Differences}, 84, 79–83. \doi{10.1016/j.paid.2014.08.021}
}
\seealso{
Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
