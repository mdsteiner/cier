% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-irv.R
\name{cier_irv}
\alias{cier_irv}
\title{Intra-individual response variability (IRV) C/IER index}
\usage{
cier_irv(responses, fpr = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{fpr}{Optional target false-positive tail mass for the percentile
cutoff. \code{NULL} (default) uses the registry default \code{0.05}. A finite number
in the open interval \verb{(0, 1)}; the cutoff is that lower-tail quantile of the
observed scores. Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional \strong{literal} cutoff on the IRV score, supplied instead
of \code{fpr}. A single finite number; respondents whose IRV is at or below it
are flagged (the lower-tail direction). Use it to apply an absolute
threshold rather than a sample percentile.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes each respondent's \strong{intra-individual response variability} -- the
sample standard deviation of their answers across items (Marjanovic et al.,
2015). A respondent who barely varies their answers (straightlining, midpoint
responding) has a low IRV, so \strong{low} values flag carelessness. The score is
computed over the raw response row (no scale blocking); items left blank are
dropped, and a respondent who answered fewer than two items abstains.
}
\details{
\strong{Cutoff.} The default flags the lowest-variability respondents: the cutoff
is the empirical \code{fpr} quantile of the observed IRV scores (the 5th
percentile by default) and respondents at or below it are flagged. This is a
\strong{ranking} convention -- a sample percentile flags \code{fpr} of respondents by
construction -- not a calibrated false-positive rate. Adjust the target tail
with \code{fpr}, or pass an absolute \code{cutoff} instead to flag on a literal IRV
threshold (e.g. one carried over from a calibration sample). \code{fpr} and
\code{cutoff} are mutually exclusive.

\strong{Abstention.} A respondent with fewer than two answered items has no
defined sample standard deviation: both \code{value} and \code{flagged} are \code{NA} and
the row is excluded from the flag count and rate. A constant respondent who
answered at least two items scores \code{0} -- the strongest low-variability
signal -- and is flagged; that is not abstention.
}
\section{What this catches}{

Straightlining and midpoint responding, which compress the response range. It
is \strong{not} useful against random responding, which inflates variability.
}

\examples{
# The 44 BFI items are the first 44 columns of the bundled example data.
out <- cier_irv(bfi_careless[, 1:44])
out
head(as.data.frame(out))
}
\references{
Marjanovic, Z., Holden, R., Struthers, W., Cribbie, R., & Greenglass, E.
(2015). The inter-item standard deviation (ISD): An index that discriminates
between conscientious and random responders. \emph{Personality and Individual
Differences}, 84, 79–83. \doi{10.1016/j.paid.2014.08.021}
}
\seealso{
\code{\link[careless:irv]{careless::irv()}}

Other indirect indices:
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_longstring]{cier_longstring()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}}
}
\concept{indirect indices}
