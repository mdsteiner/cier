% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cier-longstring.R
\name{cier_longstring}
\alias{cier_longstring}
\title{Longest-run-length C/IER index}
\usage{
cier_longstring(responses, frac = NULL, cutoff = NULL)
}
\arguments{
\item{responses}{A numeric matrix (or a data.frame / tibble coerced
internally) of responses, one row per respondent and one column per item.
\code{NA} marks a missing response.}

\item{frac}{Optional cutoff as a fraction of the item count: a finite number in
\verb{(0, 1]}, resolving to \code{ceiling(frac * p)}. \code{NULL} (default) uses \code{0.5}.
Mutually exclusive with \code{cutoff}.}

\item{cutoff}{Optional literal run-length cutoff: a finite number in \verb{[1, p]}.
Mutually exclusive with \code{frac}.}
}
\value{
A \code{cier_index}: a list with per-respondent \code{value} (numeric, \code{NA} on
abstention) and \code{flagged} (logical) vectors plus the \code{method}, \code{cutoff}, and
\code{direction} metadata. Use \link[=as.data.frame.cier_index]{as.data.frame()} for a
tidy data frame and \code{print()} for a summary.
}
\description{
Computes the maximum run length of consecutive identical responses for every
respondent (Johnson, 2005; Meade & Craig, 2012). Long runs flag straightlining.
The score is computed over the raw response row (no scale blocking).
}
\details{
Default cutoff is \code{ceiling(0.5 * p)} (\code{p} = item count) -- the "greater than
half the scale length" heuristic of Meade & Craig (2012), flagging a longest
run \code{>=} it. It is not a calibrated false-positive rate and is lenient on a
multi-scale instrument. Override with \code{frac} or \code{cutoff}.

The half-length default is over-sensitive below ~10 items, where a moderate run
is a large fraction of the scale; when it resolves to 2 or below (\code{p <= 4}) the
index warns. An all-\code{NA} row abstains (\code{value} and \code{flagged} are \code{NA}).
}
\section{What this catches}{

Straightlining (locking on one option). Not random or alternating responding,
which give short runs.
}

\examples{
# 44 BFI items; default frac = 0.5 is a run of 22 -- lenient on a multi-scale
# instrument, so compare a tighter `frac`.
out <- cier_longstring(bfi_careless[, 1:44])
out
head(as.data.frame(out))

cier_flagged_cases(out)

cier_longstring(bfi_careless[, 1:44], frac = 0.25) # cutoff = 11
}
\references{
Johnson, J. A. (2005). Ascertaining the validity of individual protocols from
web-based personality inventories. \emph{Journal of Research in Personality},
39(1), 103–129. \doi{10.1016/j.jrp.2004.09.009}

Meade, A. W., & Craig, S. B. (2012). Identifying careless responses in survey
data. \emph{Psychological Methods}, 17(3), 437–455. \doi{10.1037/a0028085}
}
\seealso{
Other indirect indices:
\code{\link[=cier_autocorrelation]{cier_autocorrelation()}},
\code{\link[=cier_even_odd]{cier_even_odd()}},
\code{\link[=cier_irv]{cier_irv()}},
\code{\link[=cier_lazr]{cier_lazr()}},
\code{\link[=cier_mahalanobis]{cier_mahalanobis()}},
\code{\link[=cier_person_total]{cier_person_total()}},
\code{\link[=cier_personal_reliability]{cier_personal_reliability()}},
\code{\link[=cier_psychant]{cier_psychant()}},
\code{\link[=cier_psychsyn]{cier_psychsyn()}}
}
\concept{indirect indices}
